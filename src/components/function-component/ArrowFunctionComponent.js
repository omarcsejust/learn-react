import React from 'react';

const ArrowFunctionComponent = (props) => {
    return(
        <div>
            <h1>Hello I am from Arrow Funtion Component {props.name}</h1>
        </div>
    );
}

export default ArrowFunctionComponent;